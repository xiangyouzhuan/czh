=== 生成不带密码的密钥对
[source, bash]
ssh-keygen -N "" -f ~/.ssh/mk

=== 上传公钥文件到远程主机
[source, bash]
ssh-copy-id -i ~/.ssh/mk.pub root@192.168.2.236

=== 在本地Linux终端中配置私钥
[source, bash]
----
cat << EOF >> ~/.ssh/config
Host 192.168.2.236
    IdentityFile ~/.ssh/mk
EOF
----

[NOTE]
如果在gitlab中添加了ssh公钥，则在克隆时必须在本地添加域名解析
