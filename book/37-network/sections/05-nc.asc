=== nc

==== nc安装
[source, bash]
----
yum install -y epel-release
yum install -y netcat
----

==== iftop使用
[source, bash]
nc --help

==== 常用命令
.扫描端口
[source, bash]
----
#检查 example.com 的 80 端口是否开放。
nc -zv nc -zv example.com 80
----
